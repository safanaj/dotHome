set logfile /home/mbardelli/.Maildirs/fetchmail.log

poll pop.araknos.it with proto POP3
     user m.bardelli@araknos.it there with password _sanajana_! is mbardelli here \
     options ssl sslcommonname *.securemail.pro sslcertck keep mda "/usr/bin/procmail -d %T"

# poll mail.nuvo.la with proto POP3
#      user mbj there with password sanajana82 is mbardelli here \
#      options ssl keep mda "/usr/bin/procmail -d %T"
